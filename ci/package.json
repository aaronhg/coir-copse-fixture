{
  "name": "coir-copse-demo-ci",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "CI gate: coir (static ClickEvent map) × copse (live reachability) over the pre-built coir-copse-demo game. No Cocos editor in the loop — it runs against ../build/web-mobile.",
  "scripts": {
    "serve": "node serve.mjs",
    "suite": "node \"${COPSE_CLI:-../../copse/src/cli.js}\" run \"${GAME_URL:-http://127.0.0.1:8899/}\" tests --junit results/junit.xml --headed",
    "gate": "node \"${ARBOR_BIN:-../../arbor/bin/arbor.mjs}\" coverage --headed",
    "gate:update": "node \"${ARBOR_BIN:-../../arbor/bin/arbor.mjs}\" coverage --update --headed",
    "visual": "node \"${ARBOR_BIN:-../../arbor/bin/arbor.mjs}\" visual --headed",
    "selftest": "node \"${ARBOR_BIN:-../../arbor/bin/arbor.mjs}\" coverage --selftest --headed",
    "check": "npm run suite && npm run gate && npm run visual && npm run selftest"
  },
  "comment": [
    "The deterministic gate is now `arbor <mode>` (coverage/visual/gate), reading ../arbor.config.mjs.",
    "The flow suite IS copse's own runner: `copse run <dir> --junit`.",
    "ARBOR_BIN / COIR_CLI / COPSE_CLI env vars point at the three sibling tools (default: relative sibling repos; CI clones them into /tmp).",
    "Local runs use --headed on purpose: headless Chrome renders Cocos via SwiftShader",
    "(software WebGL) and cooks the machine. CI is headless — ephemeral, heat irrelevant."
  ]
}
